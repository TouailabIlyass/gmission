<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.exemple.www.gmission">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".AvionActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".AddEmployeActivity" />
        <activity android:name=".EmployeActivity" />
        <activity android:name=".EmployeInfoActivity" />
        <activity android:name=".AddMissionActivity" />
        <activity android:name=".AddTransportActivity" />
        <activity android:name=".EmployeDetailActivity" />
        <activity android:name=".VehiculeActivity" />
        <activity android:name=".TrainActivity" />
        <activity android:name=".MissionInfoActivity" />
        <activity android:name=".TransportInfoActivity" />
        <activity android:name=".CtmActivity" />
        <activity android:name=".StockActivity" />
        <activity android:name=".GSTockActivity"></activity><service
        android:name=".SensorService"
        android:enabled="true" >
    </service>

        <receiver
            android:name=".SensorRestarterBroadcastReceiver"
            android:enabled="true"
            android:exported="true"
            android:label="RestartServiceWhenStopped">
        </receiver>


    </application>

</manifest>